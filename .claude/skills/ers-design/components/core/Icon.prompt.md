Stroke icon, 24x24 viewbox, Lucide-style substitute set (no codebase icon source was available).
```jsx
<Icon name="phone" size={18} color="var(--accent)" />
```
Variants: name selects from a fixed built-in path set; size/color/strokeWidth tune weight.
