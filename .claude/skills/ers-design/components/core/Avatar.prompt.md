Circular avatar with deterministic colour-from-name fallback; AvatarStack overlaps a team into a compact cluster.
```jsx
<Avatar name="Мария Волкова" status="online" />
<AvatarStack names={['Иван','Ольга','Пётр','Анна','Сергей']} max={4} />
```
