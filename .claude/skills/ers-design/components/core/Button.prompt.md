Primary call-to-action control, pill-shaped, solid accent fill.
```jsx
<Button variant="primary" size="md">Создать обращение</Button>
```
Variants: primary (solid accent), secondary (surface fill), ghost (transparent), danger (soft red). Sizes: sm/md/lg. Pass `icon` for a leading glyph/Icon element.