services:
  crm:
    build: .
    image: crmforvtb2:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - crm_data:/app/data

volumes:
  crm_data:
